# FIRE dashboard — Python dependencies
# Install with:  pip install -r requirements.txt
streamlit>=1.50
yfinance>=0.2.40
pandas>=2.0
requests>=2.31
PyYAML>=6.0
ruamel.yaml>=0.18           # comment-preserving yaml writes (watchlist edits)
python-dotenv>=1.0
Jinja2>=3.1

# Phase 2 — sentiment
vaderSentiment>=3.3.2       # default tone scorer; tiny, pure-Python
feedparser>=6.0             # Google News RSS

# Optional Phase-2 upgrade: FinBERT tone scorer
# Install separately if you want to enable settings.sentiment.scorer=finbert:
#   pip install transformers torch
